// 
// Generated by FetchLeetcodeSubmission Project on GitCafe.
// Visit https://gitcafe.com/aaronzhou/FetchLeetcodeSubmission for more information.
// Contact Me: aaron67[AT]aaron67.cc
// 
// 47. Permutations II
// https://leetcode.com/problems/permutations-ii/
// 

class Solution {
public:
    vector<vector<int> > permuteUnique(vector<int> &num) {
        set<vector<int> > s;
        sort(num.begin(), num.end());
        s.insert(num);
        while (next_permutation(num.begin(), num.end())) {
            s.insert(num);
        }
        vector<vector<int> > result;
        set<vector<int> >::iterator iter = s.begin();
        while (iter != s.end()) {
            result.push_back(*iter++);
        }
        return result;
    }
};

// 
// Given a collection of numbers that might contain duplicates, return all possible unique permutations. For example, [1,1,2] have the following unique permutations: [1,1,2], [1,2,1], and [2,1,1]. Subscribe to see which companies asked this question Show Tags Backtracking Show Similar Problems (M) Next Permutation (M) Permutations (M) Palindrome Permutation II
// 
